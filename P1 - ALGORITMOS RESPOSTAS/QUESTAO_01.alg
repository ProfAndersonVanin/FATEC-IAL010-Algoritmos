algoritmo "QUESTAO_01"
var
salario:real
salario_aumentado:real
inicio
escreval("Digite o salario atual: ")
leia(salario)
salario_aumentado<-salario*1.3
escreval("O novo salário é R$ ",salario_aumentado)
fimalgoritmo
